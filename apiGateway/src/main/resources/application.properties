server.port=8000
spring.application.name=api-gateway

# CORS configured programmatically in ApiGatewayApplication (CorsWebFilter bean)
# Accepts any origin: web, Capacitor Android/iOS, AVD emulator (http://localhost)
